<!DOCTYPE html>
<html lang="en">
<head>
  <title>TinyApp</title>
</head>
<body>
    <% if(username){ %>
    <h1>hello <%= username %> </h1>
    <% } else { %>
    <form method="POST" action="/login">
      <input type="text" name="username">
      <button>submit</button>
    </form>
    <% } %>
</body>
</html>